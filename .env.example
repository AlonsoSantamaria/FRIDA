# Local development only. Never commit real credentials.
FRIDA_ENV=local
FRIDA_LOG_LEVEL=INFO

# Configure only after the relevant access path is verified.
GOOGLE_API_KEY=
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=

